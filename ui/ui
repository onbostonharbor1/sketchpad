/* ui/uiState.js
   ------------------------------------------------------------
   Tracks the current interface state: which tab is active,
   and which div setter functions are assigned.
   ------------------------------------------------------------ */

const uiState = {
    canvasHeight: 600,
    canvasWidth:  600,
    setAction:    null,
    setButtons:   null,
    setCaption:   null,
    setSketchpad: null,
    setSubtabs:   null,
    setText:      null,

    activeTab:    "draw",      // default at startup
    activeDivs:   {},          // points to the current tab’s div map
    drawTabs:     {},
    figuresTabs:  {},
    galleryTabs:  {},
    patternsTabs: {},
    utilitesTabs: {},

    galleryManifestInfo:  null,
    patternsManifestInfo: null
};

window.uiState = uiState;  // expose globally
